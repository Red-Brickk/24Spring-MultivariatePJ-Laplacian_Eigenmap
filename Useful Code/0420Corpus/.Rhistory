plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))%>%
add_annotations(
list(
x = 1,
y = 1,
text = "This is an annotation",
showarrow = FALSE
)
)
print(plist)
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))%>%
add_legend(title = "lab", colors = unique(df_le1$colors), labels = unique(df_le1$lab))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))%>%
add_legend(title = "lab", colors = unique(df_le1$colors), labels = unique(df_le1$lab))
?add_legend
legend
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))%>%
legend(title = "lab", colors = unique(df_le1$colors), labels = unique(df_le1$lab))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"),legend = list(x = 0.1, y = 0.9))
print(plist)
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],name = df_le1$lab,type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("情態動詞","動詞","代詞","特殊名詞","介詞","名詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],name = df_le1$lab,type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[6]])
print(plist[[3]])
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
print(plist[[3]])
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","副詞","名詞、形容詞","難分辨","介詞","情態動詞、代詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
df_le1$lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","副詞","名詞、形容詞","情態動詞、代詞","難分辨","介詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[df_le1$lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[df_le1$lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
method <- "knn" # 選KNN還是ENN?
weighted <- FALSE
k <- 8 # 找幾個鄰居？
epsilon <- 0.5 # 相鄰的閾值？（初得到數據集不了解其距離尺度，需要多探索）
ndim <- 4
df_mat <- as.matrix(df, nrows=nrow(df))
# 調用函數，對矩陣，先構造圖，再找它的拉普拉斯映射
le1 <- do.lapeig(df_mat,ndim=ndim, kernelscale=tt,type=c(method,k), weighted=weighted)
# 記錄映射值
df_le1 <- as.data.frame(le1$Y)
mapname <- c("map1","map2","map3","map4","map5")
colnames(df_le1) <- mapname[1:ndim]
rownames(df_le1) <- rownames(df)
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, type = 'scatter', mode = 'markers', marker = list(color = 'rgba(0, 0, 255, 0.5)')) %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(color = 'blue', size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","副詞","名詞、形容詞","情態動詞、代詞","難分辨","介詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[df_le1$lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
print(plist)
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
print(plist[[3]])
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","副詞","名詞、形容詞","情態動詞、代詞","難分辨","特殊名詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","難分辨2","難分辨","情態動詞、代詞","aaa","特殊名詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
# Select number of clusters
k <- 6
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","難分辨2","難分辨","代詞","情態動詞","特殊名詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist[[3]])
print(plist)
# Select number of clusters
k <- 7
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("動詞過去式","難分辨2","難分辨","代詞","情態動詞","特殊名詞")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 7
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("1","2","3","4","5","6","7")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 7
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("情態動詞","特殊名詞","難分辨","過去式","代詞","系動詞、介詞","難分辨2")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
# Select number of clusters
k <- 7
set.seed(123)
# Build model with k clusters: km.out
km.out <- kmeans(df_le1, centers = k, nstart = 20)
lab <- km.out$cluster
library(dplyr)
colors <- c("red", "blue", "green", "purple","cyan","lightblue","lightgreen","black")
labnames <- c("情態動詞","特殊名詞","難分辨","動詞及過去式","代詞","系動詞、介詞","難分辨2")
# 创建散点图
plist <- list()
plist[[1]] <- plot_ly(data = df_le1, x = ~map1, y = ~map2, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim2"))
plist[[2]] <- plot_ly(data = df_le1, x = ~map1, y = ~map3, color=colors[lab], name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim3"))
plist[[3]] <- plot_ly(data = df_le1, x = ~map2, y = ~map3, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim3"))
plist[[4]] <- plot_ly(data = df_le1, x = ~map1, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim1 and dim4"))
plist[[5]] <- plot_ly(data = df_le1, x = ~map2, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list( size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim2 and dim4"))
plist[[6]] <- plot_ly(data = df_le1, x = ~map3, y = ~map4, color=colors[lab],name = labnames[lab],type = 'scatter', mode = 'markers') %>%
add_text(text = rownames(df_le1), textposition = 'top', showlegend = FALSE, hoverinfo = 'text', textfont = list(size = 10))  %>%
layout(title = paste("Laplacian Eigen Map, knn, weighted k=8, dim3 and dim4"))
print(plist)
